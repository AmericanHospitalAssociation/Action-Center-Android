<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- the theme applied to the application or activity -->
    <style name="AHATheme"
           parent="@android:style/Theme.Holo.Light.DarkActionBar">
        <item name="android:actionBarStyle">@style/AHAActionBar</item>
        <item name="android:buttonStyle">@style/AHAButton</item>

    </style>

    <!-- ActionBar styles -->
    <style name="AHAActionBar"
           parent="@android:style/Widget.Holo.Light.ActionBar.Solid.Inverse">
        <item name="android:background">@color/aha_blue</item>
        <item name="android:icon">@android:color/transparent</item>
    </style>

    <style name="AHAButton">
        <item name="android:background">@color/aha_red</item>
        <item name="android:textColor">@android:color/white</item>
        <item name="android:textSize">20dp</item>
        <item name="android:gravity">center</item>
        <item name="android:padding">@dimen/abc_button_padding_vertical_material</item>
    </style>
</resources>